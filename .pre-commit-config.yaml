repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-json
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: detect-private-key

  - repo: local
    hooks:
      - id: eslint
        name: eslint
        entry: bash -lc 'yarn -s eslint --fix --max-warnings=0'
        language: system
        files: \.[jt]sx?$

  - repo: local
    hooks:
      - id: prettier
        name: prettier
        entry: bash -lc 'yarn -s prettier --write --ignore-unknown'
        language: system
        types_or: [javascript, jsx, ts, tsx, json, css, markdown]
