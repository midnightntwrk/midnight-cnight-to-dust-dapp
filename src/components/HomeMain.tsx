'use client';

import ClientWrapper from './ui/ClientWrapper';
import Home from './Home';

export default function HomeMain() {
    return (
        <ClientWrapper>
            <Home />
        </ClientWrapper>
    );
}
